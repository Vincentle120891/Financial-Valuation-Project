backend/comps_engine.py appears to be a source code file (Python file in the backend directory), which should not be ignored according to rule #7. The change list contains only source files and no build artifacts, dependencies, or temp files.

(empty)